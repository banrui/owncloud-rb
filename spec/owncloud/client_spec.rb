require 'spec_helper'

describe Owncloud::Rb do
  it 'has a version number' do
    expect(Owncloud::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
